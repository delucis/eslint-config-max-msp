{
  "name": "eslint-plugin-max-msp",
  "description": "Max/MSP JavaScript plugin for ESLint",
  "author": "Chris Swithinbank <swithinbank@gmail.com> (http://chrisswithinbank.net/)",
  "license": "GPL-3.0",
  "scripts": {
    "prerelease": "npm t",
    "release": "standard-version -s",
    "postrelease": "npm publish && git push --follow-tags",
    "pretest": "standard",
    "test": "nyc ava",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "peerDependencies": {
    "eslint": ">=0.8.0"
  },
  "devDependencies": {
    "ava": "*",
    "commitizen": "*",
    "coveralls": "*",
    "cz-conventional-changelog": "*",
    "nyc": "*",
    "standard": "*",
    "standard-version": "*"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/delucis/eslint-plugin-max-msp.git"
  },
  "bugs": "https://github.com/delucis/eslint-plugin-max-msp/issues",
  "homepage": "https://npmjs.com/package/eslint-plugin-max-msp"
}
